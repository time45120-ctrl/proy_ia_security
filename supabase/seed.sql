-- Intentionally empty.
--
-- A clean replica must not import production users, households, devices,
-- commands, recordings, tokens, or any other personal data. Add only
-- synthetic development fixtures here when they are safe to publish.
select 1;
